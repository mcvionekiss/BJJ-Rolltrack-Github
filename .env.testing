# Testing Environment Configuration
# For local testing before deployment to staging/production

# Frontend Configuration - Support for both localhost and IP address access
# Use REACT_APP_HOST to set a specific hostname/IP, or leave blank for automatic detection
REACT_APP_HOST=localhost
REACT_APP_API_URL=${REACT_APP_HOST:+http://${REACT_APP_HOST}:8000/api}

# Backend Configuration
PIPELINE=testing  # Still use production settings but with overrides
DJANGO_SECRET_KEY=07o6r+@*h9hhvl8qfq&j@w#@q8mq2!ad4v+b@7yfvw+gm=1iu4
# Add your machine IP to these lists for local network access
ALLOWED_HOSTS=localhost,127.0.0.1,169.234.104.153,0.0.0.0
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://169.234.104.153:3000

# Database Configuration - Local MySQL or SQLite
DEBUG=False

USE_HTTPS=False
DB_NAME=rolltrack_new
DB_USER=admin
DB_PASSWORD=GymOwner2950
DB_HOST=gymdatabase.c1gma4cy6u0o.us-east-2.rds.amazonaws.com
DB_PORT=3306

# Disable HTTPS requirements for local testing
BYPASS_HTTPS=true

# Nginx Settings (if using nginx locally)
DOMAIN_NAME=localhost
USE_HTTPS=false

# This file is intended for testing production-like settings locally
# without requiring SSL certificates or domain names